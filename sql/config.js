let config = {
  //Should load from a local JSON file, these values are for testing
    host    : 'localhost',
    user    : 'NotMyUser',
    password: 'NotMYPWD',
    database: 'NotMyDB'
};

module.exports = config;